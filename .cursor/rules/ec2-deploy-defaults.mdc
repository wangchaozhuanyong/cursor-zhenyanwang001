---
description: 本仓库默认的 AWS EC2 部署信息（用户本机 SSH 用）
alwaysApply: true
---

# 生产环境 / 本机部署默认值

用户在本机从 Windows 一键 SSH 部署时，约定如下（已与 `deploy/deploy-from-windows.ps1` 默认参数一致）：

- **EC2 公有 IPv4**：`13.212.179.213`
- **SSH 私钥路径**：`E:\yamaxunmishi\aws-key.pem`（密钥目录为 `E:\yamaxunmishi`，文件名 `aws-key.pem`）
- **常见 SSH 用户**：Amazon Linux 用 `ec2-user`；若为 Ubuntu 镜像则改为 `ubuntu`
- **服务器项目根**（默认）：`/var/www/click-send-shop`

协助用户部署时：优先使用上述默认值；若用户说明已更换 IP、路径或系统镜像，再更新本规则与脚本中的默认值。

**不要**在对话中要求用户重复提供上述 IP 与密钥路径，除非用户声明已变更。
