backups/
server/.env
.env
**/.env
!**/.env.example
.env.production
**/.env.production
*.sql
.env.local

node_modules/
**/dist/
**/dist-ssr/
public-frontend/
*.log

# GitHub Actions — 忽略目录内未显式放行的文件（勿用 trailing / 忽略目录，否则无法 ! 恢复子文件）
.github/workflows/*
!.github/workflows/deploy.yml

# 部署状态文件（last_good_head / history.log 等）
.deploy-state/
deploy.log

# 本地回归/截图产物，不纳入版本库
**/artifacts/
