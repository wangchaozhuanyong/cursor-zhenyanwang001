# 放到服务器：/etc/nginx/conf.d/90-upload-body-size.conf
# 在 http { } 内生效（Ubuntu 默认已 include conf.d/*.conf），整站默认允许较大 POST（multipart 上传）。
# Nginx 内置默认仅 1m，未单独配置的 server 会导致管理后台/商品图上传 413。
client_max_body_size 60m;
