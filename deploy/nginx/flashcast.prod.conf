# 已废弃：请使用 deploy/nginx/damatong.prod.conf（damatong.net / console.damatong.net）。
# 服务器上若仍启用本文件名，install-damatong-nginx.sh 会改为启用 damatong.prod.conf。
# 不再支持 flashcast.com.my 与 cursor-main-frontend.conf（public-frontend 单域方案）。
