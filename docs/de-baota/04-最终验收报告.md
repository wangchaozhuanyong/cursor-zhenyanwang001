# 《最终验收报告》（模板）

**环境**：________  
**项目路径**：________（应为 `/var/www/...` 或 `/opt/...`，非 `/www/wwwroot`）  
**验收人 / 日期**：________

## 自动化门禁（必须全部通过）

| # | 检查项 | 命令 / 依据 | 结果 |
|---|--------|----------------|------|
| 1 | 部署脚本 | `bash deploy/production-deploy.sh` 退出码 0 | ☐ |
| 2 | verify-pm2 | `bash deploy/verify-pm2.sh` 退出码 0 | ☐ |
| 3 | PM2 入口 | `pm2 show gc-api` → script 含 `src/index.js`，非 `app.js` | ☐ |
| 4 | 监听 | `ss -ltnp \| grep 3001` → node | ☐ |
| 5 | health/live | `curl -o /dev/null -w '%{http_code}' http://127.0.0.1:3001/api/health/live` → 200 | ☐ |
| 6 | health/ready | 同上 `/api/health/ready` → 200 | ☐ |
| 7 | Nginx | `sudo nginx -t` → syntax ok | ☐ |
| 8 | 公网访问 | 浏览器打开 HTTPS 站点，首页与 API 正常 | ☐ |
| 9 | 重启恢复 | `sudo reboot` 后 `pm2 list` / `curl` 仍正常 | ☐ |
| 10 | 无宝塔依赖 | `which bt` 无；`/www/server/panel` 不存在或已卸载 | ☐ |

## 业务抽检（建议）

| # | 项 | 结果 |
|---|-----|------|
| 1 | 登录 / 权限 | ☐ |
| 2 | 下单 / 支付 mock | ☐ |
| 3 | 上传（若有） | ☐ |

## 结论

- ☐ **准予上线**（上述自动化项全部通过，且无 P0 业务缺陷）
- ☐ **不准予上线**（列出阻断项：________）

## 非阻断优化项

- ________
