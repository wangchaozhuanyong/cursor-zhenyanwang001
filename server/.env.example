PORT=3000

# —— 独立站点实例配置（同一套源码，多套独立部署）
SITE_CODE=damatong
SITE_NAME=大马通
INSTANCE_ENV=production
PM2_APP=
PROJECT_DIR=
NODE_ENV=production
TRUST_PROXY=1
PUBLIC_APP_URL=https://damatong.net
ADMIN_PUBLIC_URL=https://console.damatong.net
ADMIN_ALLOWED_ORIGINS=https://console.damatong.net
CORS_ORIGINS=https://damatong.net,https://www.damatong.net,https://console.damatong.net
ADMIN_JWT_EXPIRES_IN=15m
# 登录页接入 Turnstile 后再设为 3；未配置 ADMIN_TURNSTILE_SECRET_KEY 时此项不生效
ADMIN_LOGIN_CAPTCHA_FAILURES=0
ADMIN_LOGIN_LOCK_FAILURES=10
ADMIN_LOGIN_LOCK_MINUTES=30
# 生产默认使用 Redis 保存管理端登录风控状态；单进程应急运行才可显式设为 memory
ADMIN_LOGIN_RISK_STORE=redis
ADMIN_LOGIN_RISK_WINDOW_MINUTES=60
ADMIN_MFA_SECRET_KEY=<long-random-secret>
ADMIN_MFA_ISSUER=Damatong Admin
ADMIN_MFA_RECENT_MINUTES=10
ADMIN_TRUSTED_DEVICE_DAYS=30
DB_NAME=click_send_shop
REDIS_KEY_PREFIX=damatong
BULLMQ_PREFIX=damatong:bull
# REDIS_URL=redis://127.0.0.1:6379/0
STORAGE_KEY_PREFIX=

# API protection
# 0 disables API timeout; default is 30000 ms.
API_REQUEST_TIMEOUT_MS=30000
# Shared limiter for expensive APIs such as reports, exports, search suggestions, and batch actions.
HIGH_COST_API_RATE_LIMIT_WINDOW_MS=60000
HIGH_COST_API_RATE_LIMIT_PER_WINDOW=30

# 位于 Nginx / 负载均衡之后时建议保留默认（生产且未设为 0 时 trust proxy=1）；直连公网 Node 可设 TRUST_PROXY=0
# TRUST_PROXY=1

# 可选：更精准的城市级 IP 归属地数据库。配置 MaxMind GeoLite2-City.mmdb 文件路径后，
# 用户安全页会优先使用城市库；未配置或文件不存在时自动回退 geoip-lite。
# MAXMIND_CITY_DB_PATH=/var/lib/GeoIP/GeoLite2-City.mmdb

# HttpOnly 登录 Cookie 的 Secure 标志：1=仅 HTTPS 下发，0=允许 HTTP（本地联调）；未设置时按 X-Forwarded-Proto / req.secure 推断
# 生产 HTTPS：AUTH_COOKIE_SECURE=1；本地 HTTP：AUTH_COOKIE_SECURE=0
# AUTH_COOKIE_SECURE=1



# 生产环境：与 Node 同进程托管前端静态资源时设为 production

# NODE_ENV=production



# 前端构建目录（默认已指向仓库内 click-send-shop-main/dist）

# FRONTEND_DIST=



# 仅跑后端 API、不托管前端 dist 时设为 0（默认会托管同仓库下的前端构建产物）

# SERVE_SPA=0



# 后台 UI：默认与主站 dist 同进程托管 /admin（主站构建含管理端路由，无需单独设置）。

# 仅部署独立 admin-dist 且关闭主站 SPA 时设为 0；显式强制同主站托管 /admin 时设为 1。

# SERVE_ADMIN_SPA=0

# 独立 admin 构建目录（默认 click-send-shop-main/click-send-shop-main/admin-dist）

# ADMIN_DIST=



# 逗号分隔；须包含用户访问前端的完整 Origin（含协议与端口）。禁止使用 *。

# 本地开发可保留 localhost；生产构建校验会拒绝 CORS 中含 localhost/127.0.0.1 与占位符。

# CORS_ORIGINS 见上方实例配置；生产环境必须包含 PUBLIC_APP_URL

# 仅本地临时调试未知前端端口时开启；生产环境无效且不建议开启。
# ALLOW_DEV_CORS_ANY_ORIGIN=0



DB_HOST=localhost

DB_PORT=3306

DB_USER=root

DB_PASSWORD=

# DB_NAME 见上方实例配置；每个网站使用独立数据库



# 版本化迁移（up/down）：在 server 目录执行 npm run migrate | npm run migrate:status | npm run migrate:down

# 启动时也会自动执行未应用的迁移（见 src/index.js）



# 默认关闭首个注册用户自动升管理员，避免公网新实例被抢注接管。
# 如确需本地初始化便捷流程，可临时设为 1；生产建议使用 npm run admin:create。
# AUTO_PROMOTE_FIRST_USER_TO_ADMIN=0



# 至少 64 字符强随机；切勿提交真实密钥到版本库

JWT_SECRET=CHANGE_ME_TO_YOUR_64PLUS_CHAR_RANDOM_SECRET_FOR_DEV_AND_PROD_1234567890

JWT_EXPIRES_IN=30m



# 用户支付完成回跳、Stripe Checkout 成功/取消 URL 依赖此项；无尾部斜杠。

# 本地开发可用 http://localhost:5173；生产环境必须为 https 正式域名（见 validateEnv 校验）

# 须与用户访问前端的地址一致（本仓库 Vite 默认端口为 8080）
# PUBLIC_APP_URL 见上方实例配置

# OAuth（Google）redirect 回调须与控制台登记的「授权重定向 URI」完全一致。
# 本地示例：http://127.0.0.1:3000（与 PORT 一致，无尾部斜杠）
# OAUTH_CALLBACK_BASE_URL=http://127.0.0.1:3000
# GOOGLE_OAUTH_CLIENT_ID=
# GOOGLE_OAUTH_CLIENT_SECRET=

# 微信开放平台网站应用扫码登录（AppSecret 仅后端使用，勿暴露给前端）
# 授权回调 URI 示例：http://127.0.0.1:3000/api/auth/wechat/callback
# WECHAT_OPEN_APP_ID=
# WECHAT_OPEN_APP_SECRET=
# 设为 1 才开放微信 / Google 第三方登录（前端 THIRD_PARTY_LOGIN_ENABLED 需同步）
# THIRD_PARTY_LOGIN_ENABLED=0

# 开发环境在响应中回显 OTP（勿用于生产）
# EXPOSE_OTP_CODE=true

# 健康检查：设为 1 时 /api/health/* 返回实例详情（运维调试用，生产默认关闭）
# HEALTH_PROBE_VERBOSE=0

# 生产环境短信 OTP：启用后必须配置真实供应商，否则发送接口会拒绝。
# 支持两种通道：
# 1) Twilio REST API
# SMS_PROVIDER=twilio
# TWILIO_ACCOUNT_SID=
# TWILIO_AUTH_TOKEN=
# TWILIO_FROM=
# # 或使用 Messaging Service（与 TWILIO_FROM 二选一）
# TWILIO_MESSAGING_SERVICE_SID=
# 2) 通用 HTTP 网关（模板变量：{{phone}} / {{code}} / {{message}}）
# SMS_PROVIDER=http
# SMS_HTTP_URL=https://sms-provider.example/send
# SMS_HTTP_METHOD=POST
# SMS_HTTP_HEADERS_JSON={"Authorization":"Bearer xxx"}
# SMS_HTTP_BODY_TEMPLATE={"to":"{{phone}}","message":"{{message}}"}
# # 可选：响应正文必须匹配才视为成功
# SMS_HTTP_SUCCESS_REGEX="\"success\"\\s*:\\s*true"
# SMS_OTP_MESSAGE_TEMPLATE=Your verification code is {{code}}. It expires in 5 minutes.
# OTP_MAX_PER_IP_PER_HOUR=30
# SMS_LOGIN_ENABLED=1


# —— 订单超时关单（释放未支付订单占用的库存/活动销量/优惠券）
# 默认关闭，生产启用前请确认支付回调与关单时长策略。
# 也可在 site_settings 中配置 orderPaymentTimeoutEnabled / orderPaymentTimeoutMinutes 覆盖。
# ORDER_PAYMENT_TIMEOUT_ENABLED=1
# ORDER_PAYMENT_TIMEOUT_MINUTES=30
# ORDER_PAYMENT_TIMEOUT_INTERVAL_MS=300000



# —— Stripe（启用真实收款时二者必填；Webhook: POST /api/payment/stripe/webhook）

# STRIPE_SECRET_KEY=

# STRIPE_WEBHOOK_SECRET=


# —— Malaysia LHDN MyInvois（默认关闭；仅公司被要求或明确计划对接时开启）
# MYINVOIS_ENABLED=0
# sandbox/live；正式提交前需法务/会计完成 LHDN 测试环境验收
# MYINVOIS_SUBMIT_ENABLED=0
# MYINVOIS_API_BASE_URL=
# MYINVOIS_RETRY_INTERVAL_MS=300000


# —— Cloudflare 缓存刷新（deploy/purge-cloudflare-cache.sh / ci-deploy 可选）
# 未配置时：每次前端发版后请手动 Purge，见 deploy/release/POST-RELEASE-CHECKLIST.md
# 在 Cloudflare 控制台 → 我的个人资料 → API 令牌 → 创建「缓存清除」权限的 Token
# Zone ID：域名 Overview 页右侧栏
# CF_API_TOKEN=
# CF_ZONE_ID=
# CF_PURGE_MODE=urls
# Optional Cloudflare public bot WAF rules. Default off to avoid false positives.
# APPLY_CF_PUBLIC_BOT_RULES=0
# Optional Cloudflare Bot Management. Default off; conservative mode keeps /tiktok usable.
# APPLY_CF_BOT_MANAGEMENT=0
# CF_BOT_MANAGEMENT_MODE=conservative
# CF_BOT_MANAGEMENT_ACK=KEEP_TIKTOK_AVAILABLE
# CF_BOT_MANAGEMENT_RISK_ACK=
# Optional origin firewall. Default off; only enable after DNS is proxied through Cloudflare.
# APPLY_ORIGIN_CLOUDFLARE_ONLY=0
# ORIGIN_FIREWALL_ACK=
# ORIGIN_FIREWALL_ROLLBACK_ACK=

# Enterprise backup and point-in-time restore
BACKUP_S3_BUCKET=
BACKUP_S3_REGION=ap-southeast-1
BACKUP_S3_PREFIX=shop-backups
BACKUP_S3_ENDPOINT=
BACKUP_S3_FORCE_PATH_STYLE=0
BACKUP_S3_SSE=
BACKUP_S3_OBJECT_LOCK_MODE=GOVERNANCE
BACKUP_S3_OBJECT_LOCK_UNTIL=
BACKUP_ENCRYPTION_KEY=
BACKUP_ENCRYPTION_KEY_ID=prod-backup-key-v1
BACKUP_LOCAL_DIR=
BACKUP_BEFORE_DEPLOY=1
BACKUP_BEFORE_MIGRATION=1
MYSQL_BINLOG_DIR=/var/lib/mysql
MYSQL_BINLOG_FILES=
BACKUP_BINLOG_INCLUDE_ACTIVE=0
BACKUP_BINLOG_MIN_AGE_SECONDS=120
BACKUP_KEEP_LOCAL_ENCRYPTED=0
BACKUP_MIN_FREE_BYTES=1073741824
BACKUP_KEEP_RESTORE_WORKDIR=0
BACKUP_KEEP_RESTORE_DRILL_DB=0
RESTORE_DB_HOST=
RESTORE_DB_PORT=
RESTORE_DB_USER=
RESTORE_DB_PASSWORD=
# 设为 1 后，超级管理员可在备份中心执行「生产切换」（将临时库数据覆盖写入生产库，操作不可逆）
RESTORE_SWITCH_ENABLED=0
# 生产环境执行切换时必须在维护窗口临时设为 1，作为破坏性操作显式确认
RESTORE_SWITCH_ACK_DESTRUCTIVE=0
MYSQLDUMP_BIN=mysqldump
MYSQL_BIN=mysql
MYSQLBINLOG_BIN=mysqlbinlog
# CF_PURGE_URLS=https://example.com/favicon.ico,https://example.com/favicon.webp


# —— 对象存储（生产推荐启用）
# STORAGE_DRIVER=s3 时，商品图、Banner、站点图片、视频会上传到 S3 兼容对象存储，客户端走 STORAGE_PUBLIC_BASE_URL/CDN 访问。
# STORAGE_DRIVER 留空或非 s3 时，媒体文件会落到本机 public/uploads 或 dataURL（仅适合本地开发，不适合生产）。
# STORAGE_DRIVER=s3
# STORAGE_S3_ENDPOINT=
# STORAGE_S3_REGION=auto
# STORAGE_S3_BUCKET=
# STORAGE_S3_ACCESS_KEY_ID=
# STORAGE_S3_SECRET_ACCESS_KEY=
# STORAGE_S3_FORCE_PATH_STYLE=false
# STORAGE_PUBLIC_BASE_URL=
# STORAGE_KEY_PREFIX=damatong/prod
# 生产建议：STORAGE_KEY_PREFIX=SITE_CODE/prod，例如 damatong/prod
# 预签名直传 S3（仅 STORAGE_DRIVER=s3）：POST /api/upload/ticket → PUT S3 → POST /api/upload/complete
# UPLOAD_PRESIGN_EXPIRES_SEC=300
# Multer memoryStorage 解析前的并发内存预算保护，生产建议配合 S3 直传降低服务端内存压力
# UPLOAD_MEMORY_BUDGET_BYTES=83886080
# Video transcode worker. Start with:
# ENABLE_MEDIA_TRANSCODE_WORKER=1 pm2 start ecosystem.config.cjs --only gc-media-worker --env production --update-env
# FFMPEG_BIN=ffmpeg
# FFPROBE_BIN=ffprobe
# MEDIA_TRANSCODE_POLL_MS=60000
# MEDIA_TRANSCODE_BATCH_SIZE=1
# VIDEO_TRANSCODE_MAX_WIDTH=1280
# VIDEO_TRANSCODE_CRF=26
